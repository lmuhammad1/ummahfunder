<h1>Projects</h1>

<ul>
  <% @projects.each do |project| %>
    <li><strong><%= project.name %></strong>
    <%= project.description %> <%= number_to_currency(project.target_pledge_amount) %></li>
  <% end %>


</ul>
